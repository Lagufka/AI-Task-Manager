apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: database
  ports:
    - name: http
      port: 5432
      targetPort: 5432
